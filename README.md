# redirect tool for a/d

Фреймворк редиректов, основанный на скомпилированном приложении rinetd в докере

## Как запустить:

```bash
docker build -t swager .
chmod +x redirect.sh
./redirect.sh
```

### Поддержка:

 - Любые TCP сервисы
 - IPv6
